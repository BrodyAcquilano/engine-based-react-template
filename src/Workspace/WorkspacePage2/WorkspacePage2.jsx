function WorkspacePage2() {
  return (
    <div
      style={{
        padding: "24px",
        color: "#fff",
      }}
    >
      <h1>Workspace Page 2</h1>

      <p>
        This is an additional workspace-level page. It can be replaced with
        application preferences, account settings, global configuration, or
        another page that exists independently of engine workflows.
      </p>
    </div>
  );
}

export default WorkspacePage2;