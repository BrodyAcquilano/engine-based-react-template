function WorkspacePage1() {
  return (
    <div
      style={{
        padding: "24px",
        color: "#fff",
      }}
    >
      <h1>Workspace Page 1</h1>

      <p>
        This is the default workspace-level page. It can be replaced with a
        landing page, project manager, project selector, login page, or another
        application-level workflow that should load independently of the
        active engine.
      </p>
    </div>
  );
}

export default WorkspacePage1;